from setuptools import setup, find_packages

import fbpic_custom_sims

setup(name='fbpic_custom_sims',
      version='0.0.1',
      packages=find_packages(),
      zip_safe=False,
      include_package_data=True,
      )
